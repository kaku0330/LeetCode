package sortvowelsinastring

//"lEetcOde"
func SortVowels(s string) string {
	// var cache1, cache2 byte
	// for i := 0; i < len(s); i++ {
	// 	for j := i; j < len(s); j++ {
	// 		if s[i] < s[j] {
	// 			cache1 = s[i]
	// 			cache2 = s[j]
	// 			s[i] = cache2
	// 			s[j] = cache1
	// 		}
	// 	}
	// }
	return ""
}
